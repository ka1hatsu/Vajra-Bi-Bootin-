# Phase 7.1
Adds advanced boot configuration UI and conservative image analysis. Raw mode keeps the image layout unchanged, so non-Auto partition, target-system and filesystem overrides are blocked until later preparation backends are implemented.
